version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - ls
        - mv build .amplify-hosting
        - mv .amplify-hosting/client/assets .amplify-hosting/static
        - mkdir -p .amplify-hosting/compute
        - mv .amplify-hosting/server .amplify-hosting/compute/default
        - npm ci --omit dev
        - cp package.json .amplify-hosting/compute/default
        - cp -r node_modules .amplify-hosting/compute/default
        - cp server.js .amplify-hosting/compute/default
        - cp deploy-manifest.json .amplify-hosting/deploy-manifest.json
        - ls
        - ls .amplify-hosting/static
        - ls .amplify-hosting/compute
        - ls .amplify-hosting/compute/default
  artifacts:
    files:
      - "**/*"
    baseDirectory: .amplify-hosting